services:
  frontend:
    image: nginx:stable-alpine
    container_name: airbnb-frontend
    restart: always
    ports:
      - "80:80"
    volumes:
      # Mounts the 'build' folder from the project root
      - ./build:/usr/share/nginx/html:ro 
    # REMOVED: The custom nginx.conf volume mount is now gone.
